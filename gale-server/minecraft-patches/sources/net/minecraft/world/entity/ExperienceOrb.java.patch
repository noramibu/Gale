--- a/net/minecraft/world/entity/ExperienceOrb.java
+++ b/net/minecraft/world/entity/ExperienceOrb.java
@@ -145,18 +_,19 @@
     @Override
     public void tick() {
         this.interpolation.interpolate();
-        if (this.firstTick && this.level().isClientSide()) {
+        final Level level = this.level(); // Gale - Local code optimization - Variable extraction - ExperienceOrb.tick() - level
+        if (false) { // Gale - Local code optimization - Variable extraction - ExperienceOrb.tick() - level - isClientSide is always false
             this.firstTick = false;
         } else {
             super.tick();
-            boolean colliding = !this.level().noCollision(this.getBoundingBox());
+            boolean colliding = !level.noCollision(this.getBoundingBox()); // Gale - Local code optimization - Variable extraction - ExperienceOrb.tick() - level
             if (this.isEyeInFluid(FluidTags.WATER)) {
                 this.setUnderwaterMovement();
             } else if (!colliding) {
                 this.applyGravity();
             }
 
-            if (this.level().getFluidState(this.blockPosition()).is(FluidTags.LAVA)) {
+            if (level.getFluidState(this.blockPosition()).is(FluidTags.LAVA)) { // Gale - Local code optimization - Variable extraction - ExperienceOrb.tick() - level
                 this.setDeltaMovement(
                     (this.random.nextFloat() - this.random.nextFloat()) * 0.2F, 0.2F, (this.random.nextFloat() - this.random.nextFloat()) * 0.2F
                 );
@@ -167,10 +_,11 @@
             }
 
             this.followNearbyPlayer();
-            if (this.followingPlayer == null && !this.level().isClientSide() && colliding) {
-                boolean nextColliding = !this.level().noCollision(this.getBoundingBox().move(this.getDeltaMovement()));
+            if (this.followingPlayer == null && colliding) { // Gale - Local code optimization - Variable extraction - ExperienceOrb.tick() - level - isClientSide is always false
+                AABB boundingBox = this.getBoundingBox(); // Gale - Local code optimization - Variable extraction - ExperienceOrb.tick() - boundingBox
+                boolean nextColliding = !level.noCollision(boundingBox.move(this.getDeltaMovement())); // Gale - Local code optimization - Variable extraction - ExperienceOrb.tick() - boundingBox, level
                 if (nextColliding) {
-                    this.moveTowardsClosestSpace(this.getX(), (this.getBoundingBox().minY + this.getBoundingBox().maxY) / 2.0, this.getZ());
+                    this.moveTowardsClosestSpace(this.getX(), (boundingBox.minY + boundingBox.maxY) / 2.0, this.getZ()); // Gale - Local code optimization - Variable extraction - ExperienceOrb.tick() - boundingBox
                     this.needsSync = true;
                 }
             }
@@ -180,7 +_,7 @@
             this.applyEffectsFromBlocks();
             float friction = this.getAirDrag();
             if (this.onGround()) {
-                friction *= this.level().getBlockState(this.getBlockPosBelowThatAffectsMyMovement()).getBlock().getFriction();
+                friction *= level.getBlockState(this.getBlockPosBelowThatAffectsMyMovement()).getBlock().getFriction(); // Gale - Local code optimization - Variable extraction - ExperienceOrb.tick() - level
             }
 
             this.setDeltaMovement(this.getDeltaMovement().scale(friction));
